═══════════════════════════════════════════════════════════════
  FIREBASE & RAZORPAY CONFIGURATION - STEP BY STEP
═══════════════════════════════════════════════════════════════

📌 STEP 1: INSTALL DEPENDENCIES
───────────────────────────────────────────────────────────────
Open terminal in 'own2' directory and run:
  npm install razorpay axios

📌 STEP 2: CONFIGURE FIREBASE
───────────────────────────────────────────────────────────────
1. Go to: https://console.firebase.google.com/
2. Click "Add project" → Enter name → Create
3. Go to "Authentication" → Enable "Email/Password"
4. Go to "Project Settings" (gear icon)
5. Scroll to "Your apps" → Click Web icon (</>)
6. Register app → Copy the config object

7. Open: own2/public/signup.html
   - Find line 81-88 (firebaseConfig)
   - Replace placeholder values with your actual config

8. Open: own2/public/login.html  
   - Find line 54-61 (firebaseConfig)
   - Replace placeholder values with your actual config

📌 STEP 3: CONFIGURE RAZORPAY
───────────────────────────────────────────────────────────────
1. Go to: https://razorpay.com/
2. Sign up (free account)
3. Complete KYC (for live payments)
4. Go to Dashboard → Settings → API Keys
5. Make sure you're in "Test Mode"
6. Generate Key (if not exists)
7. Copy "Key ID" (starts with rzp_test_...)
8. Copy "Key Secret" (save it - shown only once!)

📌 STEP 4: CREATE .env FILE
───────────────────────────────────────────────────────────────
1. Create file: own2/.env
2. Add this content:

PORT=3000
MONGODB_URI=mongodb://localhost:27017/hackathon-auth
JWT_SECRET=generate-a-random-secret-key-here
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_razorpay_secret_here
FIREBASE_API_KEY=your-firebase-api-key (optional)

To generate JWT_SECRET, run:
  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

📌 STEP 5: START SERVER
───────────────────────────────────────────────────────────────
  cd own2
  node server.js

📌 STEP 6: TEST
───────────────────────────────────────────────────────────────
1. Open: http://localhost:3000/signup.html
2. Fill form and click "Sign up with Firebase"
3. Try login at: http://localhost:3000/login.html
4. As buyer, try "Buy Now" → Should open Razorpay

═══════════════════════════════════════════════════════════════
  FILES TO UPDATE:
═══════════════════════════════════════════════════════════════
✅ own2/public/signup.html (lines 81-88) - Firebase config
✅ own2/public/login.html (lines 54-61) - Firebase config
✅ own2/.env - Create this file with environment variables

═══════════════════════════════════════════════════════════════
  TEST CREDENTIALS:
═══════════════════════════════════════════════════════════════
Razorpay Test Card (Test Mode):
  Card Number: 4111 1111 1111 1111
  Expiry: 12/25 (any future date)
  CVV: 123 (any 3 digits)
  Name: Any name

═══════════════════════════════════════════════════════════════
  NEED HELP?
═══════════════════════════════════════════════════════════════
📖 See CONFIGURATION_GUIDE.md for detailed instructions
⚡ See QUICK_SETUP.md for quick reference

═══════════════════════════════════════════════════════════════



